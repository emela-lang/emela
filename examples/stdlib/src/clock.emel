-- The `std.clock` effect exposes the platform monotonic clock (specs 0013/0036).
-- Import it with `import std.clock` and call `clock.now()` inside a function
-- that declares `uses { clock }`.

effect clock {
    extern fn monotonic_seconds() -> Int

    pub fn now() -> Int {
        monotonic_seconds()
    }
}
